/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PasswortAendernComponent } from './passwort-aendern.component';

describe('Component: PasswortAendern', () => {
  it('should create an instance', () => {
    let component = new PasswortAendernComponent();
    expect(component).toBeTruthy();
  });
});
